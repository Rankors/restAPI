# Chat REST API 
## 📘 О проекте
Простой REST API для управления чатами и сообщениями.
## Стек
    Golang как язык программирования
    net/http библиотека для работы по протоколу http
    GORM для работы с базой данных 
    СУБД PostgreSQL
    testify для тестов 
## Функционал
    Создание чатов
    Отправка сообщений в определенный чат по id чата
    Получение N сообщений из чата по id чата (N не меньше 0 и не больше 100, стандарт 20 )
    Удаление чата, со всеми его сообщениями
    


## 🔗 API Endpoints

### 1. Создать чат
**POST** `/chats/`

```markdown
### Создать чат
Создаёт новый чат с указанным названием.
Передаем title внутри body.
**Запрос:**
```http
POST /chats/
Content-Type: application/json

{
  "title": "My chat"
}
```
**ответ:**
В ответ придет json, внутри есть id: id чата, который создали, а так же вернет title.  
на пустой title вернет - 400.  
```json
{
  "id": id чата,
  "title": "My chat"
}
```

### 2. Отправить сообщение в чат
**POST** `/chats/{id}/messages`
```markdown
### Отправить сообщение
Создаёт сообщение, в чате, id которого указали, в адресной строке.
Передаем text внутри body.
**Запрос:**
```http
POST /chats/
Content-Type: application/json

{
  "text": "сообщение"
}
```
**ответ:**
В ответ придет экземпляр Message, в формате json  
На несуществующий id вернет - 404.  
На пустой text вернет - 400  
```json
{
"msg_id": id сообщения, 
"chat_id": id чата, который указали,
"text": "сообщение",
"create_time": время создания,
}
```


### 3. Получить сообщение из чата
**GET** `/chats/{id}?limit=N`
```markdown
### Получение сообщений
Получение N сообщений из чата по его id (N от 1 до 100, по умолчанию 20).
**Запрос:**
```http
GET /chats/id?limit=N
Content-Type: application/json
```
**ответ:**
В ответ придет массив Message, в формате json
Если такого чата нет, то вернет - 404.  
**Пример**
```json
[ 
  { 
    "msg_id": 10, 
    "chat_id": 1, 
    "text": "Привет", 
    "create_time": "2026-01-21T10:20:25Z" 
  }, 
  { 
    "msg_id": 9, 
    "chat_id": 1, 
    "text": "Второе", 
    "create_time": "2026-01-21T10:19:10Z" 
  } 
]
```


### 3. Удаление чата
**DELETE** `/chats/{id}/`
```markdown
### Удаление чата
Получает id чата, который указали в адресной строке, и каскадно удаляет этот чат, вместе со всеми сообщения, которые в нем были.
**Запрос:**
```http
DELETE /chats/id/
Content-Type: application/json
```
**ответ:**
В ответ придет только код, успешно - 204, не найден такой чат - 404
  
  
  
  
## 🚀 Запуск проекта

### Запуск через Docker Compose
1. Установите [Docker](https://docs.docker.com/get-docker/) и [Docker Compose](https://docs.docker.com/compose/).
2. В корне проекта выполните:
   ```bash
   docker-compose up --build
   ```
3. Адреса API и СУБД:
  ```bash
  http://localhost:8080/chats
  http://localhost:5432 База данных
  ```

### 📑 Миграции
Для работы с БД используются миграции через [goose](https://github.com/pressly/goose).  
  
Применение миграций:
```bash
goose -dir migrations postgres "user=user password=password dbname=chatsdb sslmode=disable" up
```

### Тестирование
В проекте, есть тесты, {name}_test.go.  
Запуск тестов:
```bash
go test ./... -v
```

### 📂 Структура проекта
```markdown
`/code/` - Исходный код(Тесты там же).
`/migrations/` - Миграция  goose
`dockerfile` - Сборка проекта в контейнер
`docker-compose.yml` - Инструкция для Docker, что собирать и тд
`README.md` - Документация 